diff --git a/api/masque_h2.go b/api/masque_h2.go
index 84ea6d4..bc5b330 100644
--- a/api/masque_h2.go
+++ b/api/masque_h2.go
@@ -226,9 +226,11 @@ func encodeH2ConnectHeaders(u *url.URL) ([]byte, error) {
 	var headerBlock bytes.Buffer
 	encoder := hpack.NewEncoder(&headerBlock)
+	// Cloudflare's HTTP/2 fallback is a private plain CONNECT variant selected
+	// by cf-connect-proto. Advertising the RFC Capsule-Protocol extension changes
+	// how the endpoint handles the stream and breaks the full IP data plane.
 	fields := []hpack.HeaderField{
 		{Name: ":method", Value: http.MethodConnect},
 		{Name: ":authority", Value: h2Authority(u)},
-		{Name: "capsule-protocol", Value: "?1"},
 		{Name: "user-agent", Value: ""},
 		{Name: "cf-connect-proto", Value: "cf-connect-ip"},
 		{Name: "pq-enabled", Value: "false"},
diff --git a/api/masque_h2_test.go b/api/masque_h2_test.go
index 0b0361b..da63584 100644
--- a/api/masque_h2_test.go
+++ b/api/masque_h2_test.go
@@ -56,17 +56,16 @@ func TestEncodeH2ConnectHeadersUsesCloudflarePlainConnect(t *testing.T) {
 		got[field.Name] = field.Value
 	}
 	for name, expected := range map[string]string{
-		":method":          "CONNECT",
-		":authority":       "cloudflareaccess.com:443",
-		"capsule-protocol": "?1",
+		":method":          "CONNECT",
+		":authority":       "cloudflareaccess.com:443",
 		"cf-connect-proto": "cf-connect-ip",
 		"pq-enabled":       "false",
 	} {
 		if got[name] != expected {
 			t.Fatalf("header %s: got %q, want %q", name, got[name], expected)
 		}
 	}
-	for _, forbidden := range []string{":scheme", ":path", ":protocol"} {
+	for _, forbidden := range []string{":scheme", ":path", ":protocol", "capsule-protocol"} {
 		if _, exists := got[forbidden]; exists {
 			t.Fatalf("plain CONNECT must not include %s", forbidden)
 		}
diff --git a/api/transport.go b/api/transport.go
index e40cda8..a76899d 100644
--- a/api/transport.go
+++ b/api/transport.go
@@ -96,8 +96,10 @@ func (c *clientHelloFragmentConn) Write(payload []byte) (int, error) {
 	first := makeTLSRecord(payload[:3], payload[tlsRecordHeaderLength:splitAt])
 	second := makeTLSRecord(payload[:3], payload[splitAt:recordEnd])
 	if tcpConn, ok := c.Conn.(*net.TCPConn); ok {
+		// net.Dial enables TCP_NODELAY by default. Keep it enabled after the
+		// fragmented ClientHello: HTTP/2 CONNECT-IP carries latency-sensitive IP
+		// packets in small DATA frames which must not wait in Nagle's queue.
 		_ = tcpConn.SetNoDelay(true)
-		defer tcpConn.SetNoDelay(false)
 	}
 	if err := writeAll(c.Conn, first); err != nil {
 		return 0, err
